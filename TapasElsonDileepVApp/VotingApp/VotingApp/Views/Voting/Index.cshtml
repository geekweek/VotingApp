@model VotingApp.Models.MongoModel
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
@foreach (var vote in Model.ListOFVote)
{
    @Html.Label(vote.Name)
    @Html.ActionLink("Download","Download", new {id = vote.Id })
}